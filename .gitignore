__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
*.log
.DS_Store
.idea/
.vscode/
cv_run.log

# Two-DB layout (foundation/ledger.py): ledger.db is committed by the
# workflows; cache.db is rebuildable raw scan data + must stay gitignored.
cache.db
*.cache.db

# Legacy single-DB file - now superseded by ledger.db + cache.db. Kept
# ignored in case a local migration left a stray file around.
polymarketbot.db
